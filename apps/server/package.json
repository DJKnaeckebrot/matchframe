{
  "name": "server",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@workspace/game-state": "workspace:*",
    "@workspace/gsi": "workspace:*",
    "hono": "^4.9.6"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "typescript": "~6"
  }
}
